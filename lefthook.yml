pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: "*.{ts,tsx}"
      run: pnpm tsc --noEmit
    test:
      glob: "*.{ts,tsx}"
      run: pnpm test

pre-push:
  commands:
    coverage:
      run: pnpm test:coverage
